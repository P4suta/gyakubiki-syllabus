# Normalize all text files to LF, overriding core.autocrlf on any platform.
* text=auto eol=lf

# Binary assets — never normalize.
*.png binary
*.jpg binary
*.ico binary
*.woff binary
*.woff2 binary
*.wasm binary
